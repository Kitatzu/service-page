---
const { icon: Icon, title, description } = Astro.props;
---

<article class="flex flex-col gap-8 lg:px-5">
  <div class="flex justify-center">
    <span class="bg-bkg-primary rounded-full text-text-secondary p-3">
      <Icon size={"size-15"} />
    </span>
  </div>
  <div class="text-center mb-5">
    <span class="uppercase font-bold">{title}</span>
    <p class="p-4 text-lg md:text-xl leading-relaxed text-bkg-primary">
      {description}
    </p>
  </div>
</article>
